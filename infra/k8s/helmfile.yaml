repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: thesis-db
    namespace: open-thesis-radar
    chart: bitnami/postgresql
    version: "18.7.4"
    values:
      - helm-values/thesis-db-values.yml

  - name: vector-db
    namespace: open-thesis-radar
    chart: bitnami/postgresql
    version: "18.7.4"
    values:
      - helm-values/vector-db-values.yml
