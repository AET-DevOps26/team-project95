apiVersion: v1
kind: Service
metadata:
  name: genai-service
  namespace: open-thesis-radar
  labels:
    app: genai-service
spec:
  type: ClusterIP
  selector:
    app: genai-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: http
