opencollection: 1.0.0

info:
  name: Thesis Discovery API

docs:
  content: |-
    # Thesis Discovery API

    Bruno collection for the Thesis Discovery Platform API defined in `api/openapi-v1.yml`.

    Use the `Local` environment together with the local Docker Compose stack.
  type: text/markdown

ignore:
  - node_modules
  - .git
  - .env
