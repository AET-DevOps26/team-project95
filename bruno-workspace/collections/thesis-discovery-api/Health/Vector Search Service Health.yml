info:
  name: Vector Search Service Health
  type: http
  seq: 3

http:
  method: GET
  url: "{{vectorSearchServiceUrl}}/internal/v1/health"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Body contains status", function() {
          expect(res.body).to.have.property("status");
        });

settings:
  encodeUrl: true
