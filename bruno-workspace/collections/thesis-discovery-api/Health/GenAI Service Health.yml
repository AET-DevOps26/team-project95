info:
  name: GenAI Service Health
  type: http
  seq: 4

http:
  method: GET
  url: "{{genaiServiceUrl}}/actuator/health"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Body contains status", function() {
          expect(res.body).to.have.property("status");
        });

settings:
  encodeUrl: true
